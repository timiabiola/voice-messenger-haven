
import GridLayout from '@/components/layout/GridLayout';

const Saved = () => {
  return (
    <GridLayout>
      <div className="grid grid-cols-1 md:grid-cols-3 lg:grid-cols-4 gap-4">
        {/* Saved items grid will go here */}
      </div>
    </GridLayout>
  );
};

export default Saved;
